version: "3"
services:
  postgres:
    container_name: nest-tdd
    image: postgres:latest
    ports:
      - "5434:5432"
    volumes:
      - /data/postgres:/data/postgres
    env_file:
      - docker.env
    networks:
      - postgres

networks:
  postgres:
    driver: bridge